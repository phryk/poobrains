<!doctype html>
<html>
    <head>
        <title>{{ g.title }}</title>
        <link rel="stylesheet" type="text/css" href="/theme/main.css">
    </head>
    
    <body>
        <header>
        </header>
        <main>
            <h1>{{ g.title }}</h1>
            {{ content.render('full') }}
        </main>
    </body>
</html>
