<input
    type="text"
    name="{{content.prefix}}.{{ content.name }}"

    {% if content.placeholder %}
        placeholder="{{ content.placeholder }}"
    {% endif %}

    {% if content.value %}
        value="{{ content.value }}"
    {% endif %}>
