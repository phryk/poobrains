<table class="{{ content.name }}">
    <thead>
        <tr>
        {% for column in content.columns %}
            <th>{{ column }}</th>
        {% endfor %}
        </tr>
    </thead>
    <tbody>
    {% for row in content.rows %}
    <tr {% if row.classes %}class="{{ row.classes }}"{% endif %}>
        {% for column in row %}
            <td>{{ column }}</td>
        {% endfor %}
        </tr>
    {% endfor%}
    </tbody>
</table>
